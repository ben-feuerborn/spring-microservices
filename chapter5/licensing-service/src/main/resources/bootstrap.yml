spring:
    application:
     name: licensing-service 
    profiles:
      active: test
    config: 
      import: optional:configserver:http://configserver:8089

management:
  metrics:
    enable:
      all: false